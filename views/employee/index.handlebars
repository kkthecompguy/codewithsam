<div class="row">
  <div class="col-md-10">
    <h3>{{viewTitle}}</h3>
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>Job</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        {{#each employees}}
          <tr>
            <td>{{name}} </td>
            <td>{{email}} </td>
            <td>{{job}} </td>
            <td>
              <a href="/employees/edit/{{_id}}"><i class="fas fa-pencil-alt fa-lg" style="margin-right: 5px;"></i></a>
              <a href="/employees/{{_id}}" onclick="return window.confirm('Are you sure you want to delete this record?')"><i class="fas fa-trash fa-lg"></i></a>
            </td>
          </tr>
        {{else}}
          Currently our company does not have employees yet. We are on the verge of recruiting new employees
        {{/each}}
      </tbody>
    </table>
  </div>
  <div class="col-md-2">
    <a href="/employees/new" class="btn btn-secondary btn-block"><i class="fas fa-plus"></i> Create New</a>
  </div>
</div>
